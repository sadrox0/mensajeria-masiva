<Window x:Class="SIDM.Receiver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SIDM - Terminal" Height="400" Width="600"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="True" WindowStartupLocation="Manual">

	<Border Name="MainBorder" Background="#F2F2F2" CornerRadius="20" BorderBrush="#004581" BorderThickness="4">
		<Grid>
			<Grid Name="GridAlerta" Visibility="Visible">
				<Grid.RowDefinitions>
					<RowDefinition Height="50"/>
					<RowDefinition Height="*"/>
					<RowDefinition Height="70"/>
				</Grid.RowDefinitions>

				<Border Name="HeaderBar" Grid.Row="0" Background="#004581" CornerRadius="15,15,0,0" MouseLeftButtonDown="HeaderBar_MouseLeftButtonDown">
					<Grid>
						<TextBlock Text="NOTIFICACIÓN DE SISTEMA" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
						<Button Content="—" HorizontalAlignment="Right" Width="40" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="20" Click="BtnMinimizar_Click" Cursor="Hand"/>
					</Grid>
				</Border>

				<StackPanel Grid.Row="1" Margin="20" VerticalAlignment="Top">
					<TextBlock Name="txtPrioridad" Text="NIVEL" FontSize="22" FontWeight="ExtraBold"
                               HorizontalAlignment="Center" Margin="0,15,0,15"/>

					<ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="180">
						<TextBlock Name="txtMensaje" Text="Esperando mensajes..." FontSize="18"
                                   TextWrapping="Wrap" TextAlignment="Center" Foreground="#333333"/>
					</ScrollViewer>
				</StackPanel>

				<Button Grid.Row="2" Content="ENTENDIDO" Width="150" Height="40" Background="DarkGreen" Foreground="White" FontWeight="Bold" Click="BtnOk_Click" Cursor="Hand">
					<Button.Resources>
						<Style TargetType="Border">
							<Setter Property="CornerRadius" Value="10"/>
						</Style>
					</Button.Resources>
				</Button>
			</Grid>

			<Grid Name="GridBurbuja" Visibility="Collapsed" Cursor="Hand" Background="Transparent" MouseLeftButtonDown="GridBurbuja_MouseLeftButtonDown">
				<Grid Width="85" Height="85" HorizontalAlignment="Center" VerticalAlignment="Center">
					<Ellipse Name="EllipseBurbuja" Fill="#004581" Stroke="#004581" StrokeThickness="4"/>
					<Ellipse Fill="Transparent" Stroke="White" StrokeThickness="2" Margin="2"/>
					<TextBlock Text="🔔" FontSize="38" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,5"/>
					<Border Name="BadgeAlerta" Background="Red" CornerRadius="12" Width="25" Height="25"
                            HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-2,-2,0"
                            Visibility="Collapsed" BorderBrush="White" BorderThickness="2">
						<TextBlock Text="!" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontWeight="Bold" FontSize="14"/>
					</Border>
				</Grid>
			</Grid>
		</Grid>
	</Border>
</Window>